# Local emulator verification only. Production-safe code defaults are legacy/legacy.
FIRESTORE_EMULATOR_HOST=127.0.0.1:8081
GOOGLE_CLOUD_PROJECT=doujin-info-mvp
GCLOUD_PROJECT=doujin-info-mvp
METRIC_HISTORY_READ_MODE=year
METRIC_HISTORY_WRITE_MODE=year
SEARCH_INDEX_WRITE_MODE=dual

# Genre detail aggregates (local emulator).
GENRE_DETAIL_AGGREGATION_ENABLED=true
