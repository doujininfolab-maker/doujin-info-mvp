{
  "name": "doujin-info-mvp-functions",
  "version": "0.2.0",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "24"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "serve": "npm run build && firebase emulators:start --only functions,firestore",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "inspect:dlsite-product-sources": "npm run build && node lib/tools/inspectDlsiteProductSources.js",
    "test:dlsite-ajax-parser": "npm run build && node lib/tools/testDlsiteAjaxParser.js"
  },
  "dependencies": {
    "@types/node": "^24.0.0",
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0",
    "typescript": "^5.6.0"
  },
  "devDependencies": {}
}
